package de.risepos.schapfl.sync.service;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertTrue;

class GreetingResourceTest {

    @Test
    void placeholderTestForServiceModule() {
        // This project currently has no /hello endpoint; keep a tiny smoke test here
        // so stale scaffold classes from older Quarkus templates cannot break the build.
        assertTrue(true);
    }
}

